package com.pface.admin;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@RunWith(SpringRunner.class)
@SpringBootTest
public class PfaceAdminApplicationTests {

	@Test
	public void contextLoads() {
		//EntityHelper.getOrderByClause(User.class);
		//Object[] ids=new Object[]{1,2};
		//List list=  Arrays.asList(ids);
		//Long[] vehicleIds ;//=  ArrayUtil.toArray(ids);
		//vehicleIds=Arrays.stream(ids).map(p -> Long.valueOf(p.toString())).toArray(Long[]::new);
		//System.out.println(Arrays.toString(vehicleIds));
	}

}
